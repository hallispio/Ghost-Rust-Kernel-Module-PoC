[package]
name = "ghost_shell"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
# 🔥 아무것도 없음!

[build-dependencies]
bindgen = "0.69"

[profile.release]
panic = "abort"
lto = "fat"
opt-level = "z"
codegen-units = 1
overflow-checks = false

# 🔥 [핵심] compiler_builtins 완전 차단!
[profile.release.package.compiler-builtins]
opt-level = 0
codegen-units = 1