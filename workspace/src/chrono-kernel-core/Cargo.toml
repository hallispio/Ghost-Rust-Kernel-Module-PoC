[package]
name = "chrono-kernel-core"
version = "0.1.0"
edition = "2021"

[lib]
# 🔥 [핵심] C랑 합체할 때는 staticlib이 국룰입니다.
crate-type = ["staticlib"]

[dependencies]
cty = "0.2"

[build-dependencies]
bindgen = "0.69"
cc = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true