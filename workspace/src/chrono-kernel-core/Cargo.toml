[package]
name = "ghost_shell"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"] # 🔥 중요: C랑 합치려면 이게 필수!

[dependencies]
cty = "0.2"

[profile.release]
panic = "abort"
opt-level = 3
lto = true

[build-dependencies]
bindgen = "0.69" # 🔥 빌드할 때 '믹서기' 역할을 할 bindgen 추가